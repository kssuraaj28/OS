Make appropriate cr* objects in case we want to use C++
Pull TODOs from all the files
Adding useful debugging functionality to kernel shell
Make sure that only the kernel is mapped initially, and kmem, etc. isn't
Standard implementations of data structures
